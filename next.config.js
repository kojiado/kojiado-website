/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
  images: {
    domains: ['www.kojiado.com','kojiado.com','data.kojiado.com'], // Add the domains you want to allow
  },
};
